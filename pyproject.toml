[project]
name = "pos-daily-report"
version = "1.0.0"
description = "POS后台数据自动抓取 + 企业微信日报推送"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0",
    "playwright>=1.40.0",
    "pyyaml>=6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
