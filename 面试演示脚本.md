# 面试演示脚本 — POS 智能问答 Agent（演示版 8006）

> 一次 15 分钟的技术面试演示，从"开门见山"到"技术深度"层层递进。
> 所有演示用**假数据**（真实数据含 5303 个会员手机号，不能演示）。假数据与真实库**同 schema、同代码、同镜像**，只差环境变量和挂载卷。

---

## 0. 访问入口

| 端口 | 用途 | 数据 | 地址 |
|---|---|---|---|
| 8005 | 生产真实服务 | 真实库 | http://175.178.9.58:8005/ |
| 8006 | **面试演示** | 假数据 demo-data 卷 | http://175.178.9.58:8006/ |

演示前先点开 8006 首页，展示自然语言聊天界面。

---

## 1. 开场白（30 秒）

> 我帮一家儿童乐园（银豹 POS）做了一套"经营智能问答助手"：
> 用自然语言查**营收、商品销量排名、会员 RFM 分群、新客增长**，Agent 自动选工具查数据库回答。
> 今天演示用的是**模拟数据**——真实会员数据涉及隐私，演示版数据结构和真实库完全一致。

---

## 2. 核心演示问题（按顺序问，每个问题背后都有技术故事）

### 问题 ①「近一个月哪一项卖的最好？」
**预期回答**：蹦床组合乐园 3612 次/30天，远超第二名（儿童卡丁车 1118 次）。

**要讲的技术故事 — 30 天聚合 bug 修复（本次最重要）**
- 背景：这个需求看似简单，实际有个经典坑——**跨天聚合**。
- Bug 根因两层：
  1. 数据库层：原查询只取"某一天"的排名，30 天数据没聚合；
  2. 工具层：工具声明了 `days` 参数但**根本没传给数据库** → LLM 拿 8/28 单日数据当"上月"回答，曾经答成"蹦床 6 次"。
- 修复：`get_product_rankings` 增加 `days` 参数，`SUM(quantity)` + `COUNT(DISTINCT date)` + Top10 + 排除 `-`/空行 + 窗口锚定 `MAX(date)`；工具层把 `days` 真正传下去。
- **验证方法**：用与 Agent 完全相同的 SQL 手工查库对答案 → 蹦床 3787 次（真实库）/ 3612 次（演示库），一致。

### 问题 ②「这个月新增了多少会员？」
**预期回答**：2026-08 新增 549 人，比上月 234 人增加 315 人（+135%）。

**要讲的技术故事 — 统计口径 bug（演示前刚抓出来）**
- 原实现：先 `WHERE 月份过滤` 再 `MIN(trans_date)` → 统计的其实是"当月来过的会员数"（902 人），不是"新增会员"（549 人）。
- 修复：先按会员算**全时段首访日期**，再按首访月份聚合。
- 另一个坑：LLM 自己算月份不可靠（曾算出数据范围外的 2025-06），给工具加了**数据范围守卫**——问出范围外月份时明确提示覆盖范围，防止 LLM 顺着编"0 人持平"。

### 问题 ③「哪些客户是流失客户？」
**预期回答**：流失客户 325 人，消费 1-2 次且超 45 天未到店，点名（如曹丹、姜娜）。

**要讲的技术故事 — RFM 分群**
- 8 个分群（重要价值/唤回/发展/挽留 + 一般活跃/一般/新客/流失），R/F/M 相对全库均值划分。
- 5 分钟结果缓存 + 修复过一个 `_cache_key` → `cache_key` 的 NameError（第 2 次调用必崩）。

### 问题 ④「最近几个月的会员消费趋势？」
**预期回答**：6 个月持续上升，8 月 902 人到店、营收 22.4 万，3→8 月增长约 3.8 倍。

### 问题 ⑤「哪些是重要价值客户？举几个例子。」
**预期回答**：220 人，如冯艳（¥6841、25 次、1 天前）…… 高频高金额近期活跃。

---

## 3. 技术亮点速讲（如果被问"架构怎么设计的"）

1. **数据层**：SQLite 数据库是文件，**bind-mount 挂进容器**，不在镜像里。
   - 切库 = 改环境变量（`POS_DB_PATH` / `RFM_DB_PATH` / `PORT`），代码零改动。
   - 演示版 = 同一镜像 + 同一份代码 + 独立 `demo-data` 卷，真实库完全不受影响。
2. **Agent 层**：9 个工具（POS 5 + RFM 4），LangChain 函数调用，工具描述就是 LLM 的"使用说明书"。
3. **假数据设计**（`seed_demo_db.py`）：
   - 日期全部**相对今天**生成 → 任何时候跑都是最新数据，永不过期；
   - 营收：周末高（15-30k）/工作日低（8-15k）+ 每月 +8% 增长；
   - 商品：真实业务名，周末蹦床更火爆；
   - 会员：1500 人，8 个分群画像覆盖 + **入会日期按"越近越多"权重** → 每月新增会员呈平滑增长曲线（23→549 人/月）；
   - 固定随机种子 → 可复现。
4. **部署**：分层 Docker 构建（pip/Chromium 层缓存，改动仅 .py 层秒级重建）；服务器 2GB 内存，Web 容器仅 56MiB，多开一个 demo 容器无压力。
5. **测试**：23 个测试全绿，含 30 天聚合回归测试；硬编码日期已改为相对今天（修掉了 2 个静默腐烂的测试）。

---

## 4. 常见追问预答

**Q：为什么演示不用真实数据？**
A：真实库有 5303 个真实会员的姓名和手机号，属于客户隐私，不能外露。演示版用结构化假数据（schema 一致、形态贴近业务），讲能力不影响，讲隐私反而加分。

**Q：换数据源要改代码吗？**
A：不用。SQLite 是文件，通过环境变量指向：容器 A 指真实库、容器 B 指 demo 库，同一个镜像跑两份，互相隔离。

**Q：假数据怎么保证"像真的"？**
A：三个层面——① 营收有周末/工作日节律和逐月增长；② 商品用真实业务名，销量呈幂律梯队（蹦床遥遥领先）；③ 会员消费行为按分群画像生成，R/F/M 分布自然。固定随机种子保证可复现。

**Q：演示数据会过期吗？**
A：不会。所有日期相对"今天"往前推（营收 90 天、会员 180 天），每天跑种子脚本都是"最新"数据。

**Q：这个 Agent 平时在干什么？**
A：每天 23:10 定时爬取银豹日报入库，随时可以用自然语言问任何历史问题；Agent 自动决定调哪个工具、查哪段数据、怎么组织回答。

---

*部署于 2026-08-29。演示前建议：`ssh cloud` → `sudo docker exec pos-agent-demo python seed_demo_db.py`（如数据需刷新）。*
